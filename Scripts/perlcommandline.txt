perl -e 'use utf8;
use v5.12;
use strict; 
use warnings; 
use warnings  qw(FATAL utf8); 
use open      qw(:std :utf8); 
use charnames qw(:full :short);  #
use English;
my $recmark="\\lx ";
$RS = $recmark;
RECORD: while (<>) {
if ($NR== 1)  { next RECORD};
chomp;
my $count=0;
my $recstring = $recmark. $_;
$_=$recstring;
s/\\rf Stew/\\bb Stew/g;
s/\\rf SIL-AWL/\\bb SIL-AWL/g;
s/(\\xv[^\\]*\\xe[^\\]*)(\\rf[^\\]*)/$2$1/g;
print;
}
' <Nkolexbadrf 
